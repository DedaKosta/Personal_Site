import React from 'react'

const Address = (props) => {
    return (
        <>
            <div className='address'>
                <p className='left-value'>{props.value}</p>
            </div>
        </>
    )
}

export default Address
